invidious
`${INVIDIOUS_INSTANCE}/api/v1/videos/${VIDEO_ID}`

piped
`${PIPED_INSTANCE}/streams/${VIDEO_ID}`

poke
`${POKE_INSTANCE}/api/v1/videos/${VIDEO_ID}`

Yattee
${YATTEE_INSTANCE}/api/v1/videos/${VIDEO_ID}

LibreTube
${LIBRETUBE_INSTANCE}/api/v1/videos/${VIDEO_ID}

ProxiTok(TikTok用）
`${PROXITOK_INSTANCE}/api/video/${VIDEO_ID}`

Librarian（Odysee用）
`${LIBRARIAN_INSTANCE}/api/v1/video/${VIDEO_ID}`
